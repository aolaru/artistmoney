---
import BaseLayout from "../layouts/BaseLayout.astro";
import { caseStudies } from "../data/caseStudies";

const baseUrl = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const songCaseStudies = caseStudies.filter((caseStudy) =>
  caseStudy.relatedProfiles?.some((profile) => profile.type === "song")
);
---

<BaseLayout
  title="Published Song Research | How Much Music"
  description="Source-backed music case studies with song-level public records, evidence limits, and related catalog links."
>
  <section class="hero">
    <div class="hero-copy">
      <p class="eyebrow">Published Research</p>
      <h1>Source-backed questions about specific songs.</h1>
      <p>
        These articles use public records to explain what can be established about a song and what
        still requires private rights or accounting information.
      </p>
    </div>
    <div class="hero-panel" aria-label="Published song research summary">
      <p class="eyebrow">Available Now</p>
      <a class="hero-stat-pill hero-stat-pill-primary" href={`${baseUrl}case-studies/`}>
        <strong>{songCaseStudies.length}</strong>
        <span>source-backed song articles</span>
      </a>
      <a class="hero-stat-pill" href={`${baseUrl}songs/`}>
        <strong>Catalog</strong>
        <span>browse all song records</span>
      </a>
    </div>
  </section>

  <section aria-labelledby="song-case-studies-title">
    <div class="section-heading-row">
      <div>
        <p class="eyebrow">Case Studies</p>
        <h2 id="song-case-studies-title">Start with a documented question</h2>
      </div>
      <a class="home-action-link" href={`${baseUrl}case-studies/`}>Open all case studies</a>
    </div>
    <div class="card-grid case-study-grid">
      {songCaseStudies.map((caseStudy) => (
        <article class="card case-study-card">
          <div class="artist-card-body">
            <p class="eyebrow">{caseStudy.focus}</p>
            <h3><a href={`${baseUrl}case-studies/${caseStudy.slug}/`}>{caseStudy.title}</a></h3>
            <p class="card-byline">{caseStudy.artist}</p>
            <p>{caseStudy.summary}</p>
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
